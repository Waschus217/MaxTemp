<Window x:Class="MaxTemp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MaxTemp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" ResizeMode="NoResize">
    <Viewbox>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Name="lblAufforderung" Margin="0,-10,0,10">Bitte klicken, um Temperaturen auszuwerten.</Label>
            <Button Name="btnAuswerten" Click="BtnAuswerten_Click" Margin="5,13,5,39" Grid.RowSpan="3">Auswerten</Button>
            <Button Grid.Row="1" Name="btnBeenden" Click="btnBeenden_Click" Margin="5,55,0,-29" Grid.RowSpan="2" HorizontalAlignment="Left" Width="35" Height="12" FontSize="6">Beenden</Button>
            <Label Grid.Row="2" x:Name="lblAusgabe" Margin="5,13,90,10" Content="Höchst Temperatur: "/>
            <Label Grid.Row="2" x:Name="lblAverageTemp" Margin="5,34,90,-11" Content="Durchschnittliche Temperatur: "/>
            <TextBlock Grid.Row="2" x:Name="txtHighestTemperature" Margin="175,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="2" x:Name="txtAverageTemperature" Margin="175,41,0,0" VerticalAlignment="Top"/>


        </Grid>
    </Viewbox>
</Window>
